spring:

  application:
    name: userservice

  datasource:
    url: jdbc:mysql://localhost:3306/fitness-user-db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081